import BackButton from "../components/BackButton";

export default function LoginPage() {
  return (
    <main>
      <section>
        <h1>로그인 페이지</h1>
        <div>
          <BackButton />
        </div>
      </section>
    </main>
  );
}
